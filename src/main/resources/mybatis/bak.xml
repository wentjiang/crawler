<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <properties resource="mybatis/generatorConfig.properties"/>

    <classPathEntry location="D:\1.software\mysql-connector-java-5.1.34.jar"/>
    <context id="MysqlTables" targetRuntime="MyBatis3">
        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>
        <jdbcConnection driverClass="${jdbc_driver}"
                        connectionURL="${jdbc_url}"
                        userId="${jdbc_username}"
                        password="${jdbc_password}"/>
        <javaModelGenerator targetPackage="${modelPackage}" targetProject="${targetProject}">
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>
        <sqlMapGenerator targetPackage="${sqlMapperPackage}" targetProject="${targetProject}"/>
        <javaClientGenerator targetPackage="${daoMapperPackage}" targetProject="${targetProject}" type="XMLMAPPER"/>

        <!-- table info -->
        <table tableName="db_book" domainObjectName="Book">
            <property name="keyProperty" value="id"/>
        </table>

        <table tableName="db_book_tag" domainObjectName="BookTag">
            <property name="keyProperty" value="id"/>
        </table>

        <table tableName="db_book_tag_mapping" domainObjectName="BookBookTag">
            <property name="keyProperty" value="book_id"/>
            <property name="keyProperty" value="book_tag_id"/>
        </table>
    </context>


</generatorConfiguration>